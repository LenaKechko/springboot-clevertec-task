spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: "jdbc:postgresql://localhost:5432/myHibernateDatabase"
    username: "postgres"
    password: "pass"
  jpa:
    show-sql: true

hibernate:
  dialect: "org.hibernate.dialect.PostgreSQLDialect"
  show_sql: true

server.port: 8080

#liquibase:
#  change-log: db/changelog/changelog.xml
#  enable: true
